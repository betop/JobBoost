## Update SwiftCV and API

# Bidders
- need to check all status (match, mismatch, unfit, reposted, duplicated, not_a_jd, ai_error, etc.)
- able to generate resumes for only match jobs
- for other status, just show status and reason with close/ok button

# Admins & Super Admin
- need to check all status (match, mismatch, unfit, reposted, duplicated, not_jd, ai_error, etc.)
- able to generate resumes for match jobs ofc.
-  also able to generate anyway for mismatch, unfit, reposted, duplicated, not_jd except ai_error but display status and reason
